module.exports = {
  semi: true, // 문장 끝에 세미콜론(;)을 붙임
  singleQuote: true, // 문자열은 작은따옴표(')로
  trailingComma: 'es5', // ES5에서 유효한 곳(객체, 배열, 함수 인자 등)에만 콤마
  printWidth: 100, // 한 줄 최대 길이 (코드 가독성용)
  tabWidth: 2, // 들여쓰기 2칸
  bracketSpacing: true, // { foo: bar } 형태로 공백 유지
  arrowParens: 'avoid', // 화살표 함수 인자 1개일 때 괄호 생략
};
